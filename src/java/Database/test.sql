CREATE TABLE FilterDB;

(
id int NOT NULL AUTO_INCREMENT,
filter_name varchar(50),
filter_amount varchar(50),
filter_number int,
filter_title varchar(50),
filter_meow varchar(50),
filter_date date,
selection varchar(10),
PRIMARY KEY (id)
);